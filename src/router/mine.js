import React from 'react';
import {BrowserRouter as Router} from 'react-router-dom';

export default class MineRoute extends React.Component {
  
  render() {
    return (
      <Router/>
    );
  }
  
}